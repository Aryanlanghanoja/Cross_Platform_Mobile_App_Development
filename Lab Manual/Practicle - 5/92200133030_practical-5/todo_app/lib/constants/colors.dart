import 'package:flutter/material.dart';

// Define a constant color for red, typically used for errors or alerts
const Color tdRed = Color(0xFFDA4040);

// Define a constant color for blue, can be used for primary buttons or highlights
const Color tdBlue = Color(0xFF5F52EE);

// Define a constant color for black, useful for text or dark backgrounds
const Color tdBlack = Color(0xFF3A3A3A);

// Define a constant color for grey, often used for disabled elements or secondary text
const Color tdGrey = Color(0xFF717171);

// Define a constant background color, providing a light theme to the app
const Color tdBGColor = Color(0xFFEEEFF5);
